<Application
    x:Class="Paris_Saveur.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Paris_Saveur">
    
    <Application.Resources>
        <ResourceDictionary>
            <DataTemplate x:Key="RestaurantListViewItemTemplate">
                <Grid Background="White">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1*"></ColumnDefinition>
                        <ColumnDefinition Width="4*"></ColumnDefinition>
                    </Grid.ColumnDefinitions>
                    <Image x:Name="restaurantThumbnail" Grid.Column="0" Source="{Binding ThumbnailBitmap}" Margin="5,5,5,5"></Image>
                    <StackPanel x:Name="restaurantSimpleInfoStack" Grid.Column="1" Orientation="Vertical" HorizontalAlignment="Stretch">
                        <TextBlock x:Name="restaurantSimpleInfoTitle" FontSize="15" Foreground="Black" Text="{Binding name}" FontWeight="Bold"></TextBlock>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="2*"></ColumnDefinition>
                                <ColumnDefinition Width="2*"></ColumnDefinition>
                            </Grid.ColumnDefinitions>
                            <TextBlock x:Name="restaurantSimpleInfoType" Grid.Column="0" FontSize="15" Foreground="Black" Text="{Binding style}" HorizontalAlignment="Left"></TextBlock>
                            <TextBlock x:Name="restaurantSimpleInfoPrice" Grid.Column="1" Foreground="Black" FontSize="15" Text="{Binding consumption_per_capita}" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,10,0"></TextBlock>
                        </Grid>
                        <TextBlock x:Name="restaurantSimpleInfoReview" FontSize="15" Foreground="Black" Text="{Binding RatingScoreAndReviewNum}"></TextBlock>
                    </StackPanel>
                </Grid>
            </DataTemplate>
        </ResourceDictionary>
    </Application.Resources>
</Application>