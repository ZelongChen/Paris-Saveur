<Page
    x:Class="Paris_Saveur.HotRestaurantPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Paris_Saveur"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <ScrollViewer Background="White">
        <StackPanel Orientation="Vertical">
            <ProgressBar x:Name="LoadingBar" Visibility="Collapsed" IsEnabled="False" IsIndeterminate="true" Height="4" HorizontalAlignment="Stretch" Margin="0,300,0,0"/>
            <ListView x:Name="hotRestaurantList" 
                  Grid.Row="0" 
                  Grid.Column="0"
                  ItemsSource="{Binding}"
                  ItemTemplate="{StaticResource RestaurantListViewItemTemplate}"
                  HorizontalContentAlignment="Stretch"
                  SelectionChanged="hotRestaurantList_SelectionChanged">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"></Setter>
                    </Style>
                </ListView.ItemContainerStyle>
            </ListView>
            <Button x:Name="loadMoreButoon"
				    Background="White"
                    HorizontalContentAlignment="Stretch"
                    Content="加载更多"
                    Foreground="Black"
                    HorizontalAlignment="Center"
				    Click="loadMoreButton_Click" VerticalAlignment="Bottom"/>
        </StackPanel>
    </ScrollViewer>
    <Page.BottomAppBar>
        <CommandBar ClosedDisplayMode="Compact" Opacity="0.5">
            <AppBarButton Label="排序" Icon="Sort">
                <AppBarButton.Flyout>
                    <MenuFlyout>
                        <MenuFlyoutItem Text="热门程度" Click="SortByPopularity_Click"/>
                        <MenuFlyoutItem Text="综合评分" Click="SortByRatingScore_Click"/>
                        <MenuFlyoutItem Text="点评数量" Click="SortByRatingNum_Click"/>
                    </MenuFlyout>
                </AppBarButton.Flyout>
            </AppBarButton>
        </CommandBar>
    </Page.BottomAppBar>
</Page>
