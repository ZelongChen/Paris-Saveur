<Page
    x:Class="Paris_Saveur.NearByRestaurant"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Paris_Saveur"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <ScrollViewer Background="White">
        <StackPanel Orientation="Vertical">
            <ProgressBar x:Name="LoadingBar" Visibility="Collapsed" IsEnabled="False" IsIndeterminate="true" Height="4" HorizontalAlignment="Stretch" Margin="0,300,0,0"/>
            <ListView x:Name="nearbyRestaurantList" 
                  Grid.Row="0" 
                  Grid.Column="0"
                  ItemsSource="{Binding}"
                  ItemTemplate="{StaticResource RestaurantListViewItemTemplate}"
                  HorizontalContentAlignment="Stretch"
                  SelectionChanged="nearbyRestaurantList_SelectionChanged">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"></Setter>
                    </Style>
                </ListView.ItemContainerStyle>
            </ListView>
            <Button x:Name="loadMoreButoon"
				    Background="White"
                    HorizontalContentAlignment="Stretch"
                    Content="加载更多"
                    Foreground="Black"
                    HorizontalAlignment="Center"
				    Click="loadMoreButton_Click" VerticalAlignment="Bottom"/>
        </StackPanel>
    </ScrollViewer>
    <Page.BottomAppBar>
        <CommandBar ClosedDisplayMode="Compact" Opacity="0.5">
            <AppBarButton Label="地图" Icon="Map" Click="OpenMap_Click"></AppBarButton>
            <AppBarButton Label="刷新" Icon="Refresh" Click="Refresh_Click"></AppBarButton>
        </CommandBar>
    </Page.BottomAppBar>
</Page>
